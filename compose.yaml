services:
  postgres_db:
    image: postgres:14
    environment:
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: Kevinsex250806@
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - canchaya_data:/var/lib/postgresql/data

volumes:
  canchaya_data:
